#include <bits/stdc++.h>

#define fi first
#define se second

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    double t;
    int n;
    cin >> t >> n;

    cout << setprecision(3);
    cout << fixed;
    cout << t / n << endl;
    cout << n * 2;

    return 0;
}

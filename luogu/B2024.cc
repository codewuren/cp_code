#include <bits/stdc++.h>

#define fi first
#define se second

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    double num;
    cin >> num;

    printf("%f\n%.5f\n%e\n%g", num, num, num, num);

    return 0;
}

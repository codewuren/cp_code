#include <bits/stdc++.h>

#define fi first
#define se second

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    char ch = getchar();
    cout << char(ch - 32) << endl;

    return 0;
}
